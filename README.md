# Music-bot-with-request-channel
1 เปลียนชื่อ `example.env` => `.env` 
2 ใส่สิ่งที่จำเป็นทั้งหมด 1token 2prefix 3idเจ้าของ 4mongodb 5port(เลข 4 ตัวอะไรก็ได้) 6host-port-pass-secureคือLavalinkเลือกได้ใน `lavalink_server.js` 7spotifyID-spotifySecret(ใน[spotify dev hub](https://developer.spotify.com/dashboard/login))
