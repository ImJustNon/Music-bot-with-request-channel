# Music-bot-with-request-channel
-- เปลียนชื่อ `example.env` => `.env` 
-- ใส่สิ่งที่จำเป็นทั้งหมด 
  1) token 
  2) prefix 
  3) idเจ้าของ 
  4) mongodb 
  5) port(เลข 4 ตัวอะไรก็ได้) 
  6) host-port-pass-secureคือLavalinkเลือกได้ใน `lavalink_server.js` 
  7) spotifyID-spotifySecret(ใน[spotify dev hub](https://developer.spotify.com/dashboard/login))
